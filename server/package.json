{
  "name": "blog-app",
  "version": "0.0.1",
  "description": "",
  "main": "src/server.js",
  "type": "module",
  "scripts": {
    "start": "cross-env NODE_ENV=production node --experimental-specifier-resolution=node src/server.js",
    "dev": "cross-env NODE_ENV=development nodemon --experimental-specifier-resolution=node src/server.js",
    "debug": "cross-env DEBUG=express* NODE_ENV=development nodemon --inspect --experimental-specifier-resolution=node src/server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "ngrink",
  "license": "ISC",
  "dependencies": {
    "bcrypt": "^5.1.0",
    "cross-env": "^7.0.3",
    "dotenv": "^16.0.3",
    "express": "^4.18.2",
    "express-fileupload": "^1.4.0",
    "express-validator": "^6.14.2",
    "joi": "^17.6.3",
    "jsonwebtoken": "^8.5.1",
    "mongoose": "^6.6.5"
  },
  "devDependencies": {
    "nodemon": "^2.0.20"
  }
}
